<?xml version="1.0"?>
<!DOCTYPE Configure PUBLIC "-//Jetty//Configure//EN" "http://www.eclipse.org/jetty/configure_9_3.dtd">
 
<Configure>
    <!-- =========================================================== -->
    <!-- Server Thread Pool                                          -->
    <!-- =========================================================== -->
    <New class="org.eclipse.jetty.util.thread.QueuedThreadPool">
      <!-- initial threads set to 10 -->
      <Set name="minThreads">10</Set>
 
      <!-- the thread pool will grow only up to 200 -->
      <Set name="maxThreads">200</Set>
 
      <!-- indicates that having 20 and below, the pool will be considered low on threads -->
      <Set name="lowThreadsThreshold">20</Set>
 
      <!-- the number of reserved threads -->
      <Set name="reservedThreads">2</Set>
    </New>

    <New id="Server" class="org.eclipse.jetty.server.Server">
      <Arg name="threadpool"><Ref refid="threadPool" /></Arg>
    </New>
</Configure>
